import React from 'react';

/**
 * PermissionModeSelector - disabled for pi-agent-core refactor.
 * Permission system is not implemented in the first phase.
 */
const PermissionModeSelector: React.FC<{ sessionId?: string }> = () => {
  return null;
};

export default PermissionModeSelector;
